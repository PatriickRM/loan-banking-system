spring:
  application:
    name: CreditEvaluationService

  config:
    import: optional:configserver:http://localhost:8100

  cloud:
    config:
      enabled: true
      fail-fast: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true